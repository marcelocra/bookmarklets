/**
 * Google Calendar - Increase width of Google Tasks pane.
 */
javascript:(function(){  if (!window.location.hostname.includes('calendar.google.com')) {    alert('This bookmarklet only works on Google Calendar');    return;  }    const screenWidth = window.screen.width;  let newWidth;    if (screenWidth <= 1366) {    newWidth = '400px';  } else if (screenWidth <= 1920) {    newWidth = '500px';  } else {    newWidth = '600px';  }    let override = prompt(`Will set to '${newWidth}'. Override? (Type the value as a number.)`); document.documentElement.style.setProperty(%27--companion-shell-width%27, override ? `${override}px` : newWidth);})();
